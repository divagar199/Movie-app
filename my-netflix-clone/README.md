# 🎬 Netflix Clone (MERN Stack & Vite)

A fully responsive Netflix Clone application built using **React (Vite)**, **Tailwind CSS**, and **Express.js**. This project features user authentication (Login/Signup), a dynamic movie dashboard, and integration with the TMDB API for real-time movie data.

## 🚀 Features

- **Authentication:** Custom Login and Signup flows using an Express backend.
- **Dynamic Dashboard:** Fetches trending movies, top-rated content, and genre-specific categories.
- **Auto-Refreshing Hero Banner:** The main banner image automatically cycles through trending Netflix Originals every 5 seconds.
- **Responsive Design:** Fully responsive UI built with Tailwind CSS (Mobile & Desktop friendly).
- **Real-time Data:** Integrated with The Movie Database (TMDB) API.

## 🛠️ Tech Stack

### Frontend
- **Framework:** React.js (via Vite)
- **Styling:** Tailwind CSS (v4)
- **Routing:** React Router DOM
- **HTTP Client:** Axios
- **Icons:** Lucide React

### Backend
- **Server:** Node.js & Express.js
- **Middleware:** Cors, Body-Parser
- **Data:** In-memory mock storage (for user credentials)

---

## ⚙️ Installation & Setup

Follow these steps to run the project locally.

### 1. Clone the Repository
```bash
git clone divagar199>
cd my-netflix-clone